<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>confirm</title>
    <style>
        table {
            border: 1px solid black;
            width: 50%;
            height: 500px;
            background-color: brown;
            color: azure;
        }
    </style>
</head>
<body>

    <form action="/latest/event/<%= eventID._id %>?_method=DELETE" method="POST">
        <table>
            <tr>
                <td>ID</td>
                <td><%= eventID._id %></td>
            </tr>
            <tr>
                <td>Title</td>
                <td><%= eventID.title %></td>
            </tr>
            <tr>
                <td>Gallery Wrap</td>
                <td><%= eventID.gallerywrap %></td>
            </tr>
            <tr>
                <td>Bio</td>
                <td><%= eventID.bio %></td>
            </tr>
            <tr>
                <td>Description</td>
                <td><%= eventID.description %></td>
            </tr>
            <tr>
                <td>Date</td>
                <td><%= eventID.date %></td>
            </tr>
    
        </table>
        <button>delete</button>
    </form>
    
</body>
</html>